# Nano Banana Studio 사용 설명서

## 1. 설치와 실행

### 처음 한 번 설치

1. Node.js와 Python을 설치합니다.
2. 이 저장소를 내려받아 압축을 풉니다.
3. 앱 폴더에서 다음 명령을 실행합니다.

```powershell
npm install
python -m pip install -r requirements.txt
```

### 실행 파일 선택

- `실행하기.bat`: 기존 1회 분석 화면을 엽니다.
- `실행하기-새하네스.bat`: 새 분석 하네스 화면을 엽니다.
- `실행하기-비교.bat`: 두 분석 방식을 나란히 비교하는 화면을 엽니다.

원하는 BAT 파일을 더블클릭하면 앱 창이 열립니다. 앱 창을 닫으면 함께 실행된 로컬 엔진도 종료됩니다.

## 2. 처음 사용할 때

1. 오른쪽 위의 설정 버튼을 누릅니다.
2. `Gemini API 키`에 자신의 키를 입력합니다.
3. 필요하면 `API 모델 전체 새로고침`을 눌러 해당 키에서 제공되는 모델을 불러옵니다.
4. 분석 모델과 이미지 생성 모델을 선택합니다.

API 키는 앱이 열려 있는 동안 세 화면에서 함께 유지됩니다. 앱을 닫거나 페이지를 새로고침하면 지워집니다.

## 3. 화면 선택

상단 메뉴에서 다음 화면을 선택할 수 있습니다.

### 기존 분석

이미지 한 장을 한 번 분석해 상세 분석 사양과 한국어·영어 재구성 프롬프트를 만듭니다.

### 새 하네스

같은 선택 모델로 다음 세 단계를 순서대로 실행합니다.

1. 이미지에서 확인되는 증거 수집
2. 증거의 모순과 과도한 추정을 교차 비평
3. 최종 분석과 재구성 프롬프트 합성

### 비교

같은 이미지, 모델, Agentic Vision 설정으로 기존 분석과 새 하네스를 함께 실행해 결과를 나란히 보여줍니다.

## 4. 설정 메뉴

### Gemini API 키

- 눈 모양 버튼으로 키 표시 여부를 바꿉니다.
- `API 모델 전체 새로고침`은 현재 키에서 사용할 수 있는 모델 목록을 다시 불러옵니다.

### 분석 모델

이미지 분석에 사용할 모델을 선택합니다. 기본값은 지능 우선의 `gemini-pro-latest`입니다. 앱은 선택한 모델을 증거 조사, 비평, 종합을 포함한 모든 분석 단계에 그대로 사용하며 다른 모델로 자동 변경하지 않습니다.

앱을 켜면 다음 12개 분석 모델이 기본 목록에 표시됩니다.

- `gemini-pro-latest`
- `gemini-3.1-pro-preview`
- `gemini-3.6-flash`
- `gemini-3.5-flash`
- `gemini-3.5-flash-lite`
- `gemini-3-flash-preview`
- `gemini-3.1-flash-lite`
- `gemini-flash-latest`
- `gemini-flash-lite-latest`
- `gemini-2.5-pro`
- `gemini-2.5-flash`
- `gemini-2.5-flash-lite`

목록의 표시는 출처에 따라 `[기본]`, `[계정]`, `[Custom]`으로 구분됩니다.

- `[기본]`: API 조회 전에도 바로 선택할 수 있는 기본 목록입니다.
- `[계정]`: `API 모델 전체 새로고침`으로 현재 API 키에서 조회된 모델입니다. 앱은 모든 페이지를 조회하고 `generateContent`를 지원하는 Gemini 모델을 중복 없이 합칩니다. 이미지 생성, Veo, TTS, Live·Audio, Embedding, Robotics·Computer Use, Gemma·Lyria와 전용 Agent 계열은 분석 선택기에서 제외됩니다.
- `[Custom]`: `Custom Model ID`에 모델명을 직접 입력하고 `추가·선택`을 누르면 사용합니다. `models/` 접두사는 입력해도 자동으로 제거됩니다. 계정에서 사용할 수 없는 모델이면 다른 모델로 우회하지 않고 API 오류를 표시합니다.

모델 조회에 실패해도 기본 목록과 기존 Custom 항목은 유지되며, 조회 실패 이유가 설정 화면에 표시됩니다.

### 코드 기반 정밀검사 허용

Agentic Vision을 사용하도록 허용합니다. 켜더라도 모델이 필요하다고 판단할 때만 확대, 좌표 또는 픽셀 검사를 실행합니다. 선택한 분석 모델은 바뀌지 않습니다. 실제 실행 여부와 검사 횟수는 분석 리포트에서 확인합니다.

### 이미지 생성 모델

추출한 프롬프트로 이미지를 생성할 때 사용할 모델을 선택합니다.

### 화면 비율과 생성 해상도

생성 이미지의 화면 비율과 `1K`, `2K`, `4K` 해상도를 선택합니다.

### 전체 모델 카탈로그

분석 선택 가능 모델, 이미지 생성 선택 가능 모델, 현재 작업에 사용하지 않는 전용 모델을 확인합니다.

## 5. 이미지 분석

1. 사용할 작업공간을 클릭해 활성화합니다.
2. `원본 이미지` 영역을 클릭해 파일을 선택하거나, 이미지를 드래그하거나, 클립보드 이미지를 붙여넣습니다.
3. 이미지가 들어오면 분석이 자동으로 시작됩니다.
4. 완료되면 `분석 사양`과 `생성 프롬프트`가 표시되고 히스토리에 자동 저장됩니다.

원본 이미지 위의 버튼으로 크게 보기, 다운로드, 제거를 할 수 있습니다. `다시 분석`은 현재 이미지와 현재 설정으로 분석을 다시 실행합니다.

붙여넣기는 현재 활성화한 작업공간에 적용됩니다. 활성 작업공간이 없으면 비어 있는 작업공간이 우선 선택됩니다.

## 6. 분석 사양과 프롬프트

### 분석 사양

- `EN`과 `KO`로 영어·한국어 분석을 전환합니다.
- 분석 내용은 직접 편집할 수 있습니다.
- `복사`로 현재 표시된 분석을 클립보드에 복사합니다.

### 생성 프롬프트

- `EN`과 `KO`로 영어·한국어 프롬프트를 전환합니다.
- 프롬프트는 이미지 생성 전에 직접 편집할 수 있습니다.
- `프롬프트 복사`로 현재 프롬프트를 복사합니다.
- `저장`으로 현재 편집 상태를 히스토리에 저장합니다.

언어를 전환하면 해당 언어의 원래 분석 결과가 편집기에 다시 들어갑니다. 직접 수정할 경우 먼저 사용할 언어를 선택한 뒤 편집합니다.

## 7. 프롬프트로 이미지 생성

1. 설정에서 이미지 생성 모델, 화면 비율, 해상도를 선택합니다.
2. 생성 프롬프트를 확인하거나 수정합니다.
3. `프롬프트로 이미지 생성`을 누릅니다.
4. 생성 결과가 오른쪽 미리보기에 표시되고 히스토리가 갱신됩니다.

생성 이미지 위의 버튼으로 크게 보거나 다운로드할 수 있습니다.

## 8. 작업공간

- `새 작업 공간 추가`로 작업공간을 계속 추가할 수 있습니다.
- 작업공간 제목줄의 휴지통 버튼으로 해당 작업공간을 닫습니다.
- 최소 한 개의 작업공간은 유지됩니다.
- 작업공간을 닫아도 이미 저장된 히스토리는 삭제되지 않습니다.
- 기존 분석과 새 하네스 화면은 서로 별도의 작업공간 상태를 유지합니다.

## 9. 비교 화면 사용법

1. API 키와 분석 모델을 확인합니다.
2. 필요한 경우 `코드 기반 정밀검사 허용`을 켭니다.
3. 이미지를 클릭, 붙여넣기 또는 드래그로 넣습니다.
4. `동일 조건 A/B 분석 실행`을 누릅니다.

두 결과에는 각각 생성 프롬프트, 한국어 분석 사양, 처리 시간, 토큰, 비용과 분석 리포트가 표시됩니다. 성공한 각 결과는 히스토리에 따로 저장됩니다.

- 한쪽만 실패하면 `실패 단계부터 재시도`로 해당 방식의 실패 단계와 이후 단계만 다시 실행합니다. 재시도 전에 모델을 직접 바꿀 수 있으며, 이미 성공한 단계는 다시 호출하지 않습니다.
- `비교 리포트 .md 저장`으로 두 결과의 리포트를 하나의 Markdown 파일로 저장합니다.
- 분석 모델을 바꾸면 완료된 이전 결과는 초기화되지만 실패 지점은 재개할 수 있게 유지됩니다. Agentic Vision 설정을 바꾸면 분석 조건 자체가 달라지므로 이전 결과와 실패 지점이 모두 초기화됩니다.

## 10. 분석 실행 리포트

작업공간 또는 비교 결과의 `분석 실행 리포트`를 펼치면 다음 항목을 확인할 수 있습니다.

- 성공, 실패 또는 거절 결과
- API 호출 방식과 Safety 설정
- 사용자가 선택한 모델과 API가 응답한 실제 모델
- Agentic Vision 선택 여부와 실제 실행 상태
- 정밀검사 횟수, 검사 영역, 목적과 결과
- 단계별 실행 시간과 각 API 시도의 모델·시간·종료 상태
- 입력·출력·생각·도구 사용 토큰
- 전체 추정 비용과 Agentic Vision 직접 귀속 추정 비용
- 종료 사유, 프롬프트 차단 사유와 실패 이유

`리포트 .md 저장`으로 현재 리포트를 Markdown 파일로 저장합니다. 가격표에 없는 모델은 비용이 `산정 불가`로 표시되며 토큰 기록은 그대로 유지됩니다.

## 11. 히스토리

왼쪽의 `History`에서 저장된 결과를 관리합니다. 작은 화면에서는 왼쪽 위 히스토리 버튼으로 엽니다.

- 기록을 클릭하면 비어 있는 작업공간 또는 첫 번째 작업공간으로 불러옵니다.
- 검색창에 프롬프트나 분석 문구를 입력하고 Enter를 눌러 검색합니다.
- 휴지통 버튼으로 기록 하나를 삭제합니다.
- `더 불러오기`로 이전 기록을 추가로 표시합니다.
- `지속 저장 요청`으로 브라우저에 로컬 데이터 유지 권한을 요청합니다.
- 저장소 카드에서 현재 사용량과 할당량을 확인합니다.
- `전체 원본 + 분석 ZIP 내보내기`로 모든 기록을 ZIP 파일로 저장합니다.
- `전체 삭제`는 확인 후 모든 히스토리와 저장 이미지를 삭제합니다.

ZIP에는 각 기록의 원본·생성 이미지, 프롬프트, 분석 텍스트·JSON, 실행 리포트 Markdown·JSON과 기록 정보가 포함됩니다.

## 12. 오류와 재시도

- 일시적인 네트워크 오류, HTTP 408·429·5xx와 일시적인 생성 중단은 최초 요청 뒤 최대 두 번 자동 재시도합니다.
- 자동 재시도는 같은 모델·이미지·설정을 그대로 사용합니다.
- HTTP 400·401·403 등 반복해도 해결되지 않는 요청·인증 오류는 즉시 실제 오류 내용을 표시합니다.
- 기존 분석은 유일한 분석 단계부터 다시 시도합니다. 새 하네스는 `실패 단계부터 재시도`로 성공한 앞 단계를 보존하고 실패 단계부터 이어갑니다.
- 새 하네스와 비교 화면에서는 재시도 전에 사용자가 모델을 직접 바꿀 수 있습니다. 새 모델은 실패 단계와 이후 단계에만 적용되며 자동 모델 폴백은 하지 않습니다.
- 실패한 호출도 시도 횟수, 시간, 모델, 응답이 있는 경우 토큰, 종료 사유와 오류 이유가 리포트에 남습니다.
- Agentic Vision을 지원하지 않는 모델에서 오류가 발생하면 설정에서 정밀검사를 끄거나 지원 모델을 선택해 다시 실행합니다.
